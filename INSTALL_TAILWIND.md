# Panduan Instalasi Tailwind CSS

Project ini ditambahkan Tailwind CSS. Karena folder `node_modules` tidak disertakan dalam repository, Anda perlu melakukan instalasi ulang dependensi untuk memunculkan folder tersebut.

## Langkah-langkah:

1. **Install Tailwind CSS**
   Install tailwindcss and @tailwindcss/cli via npm.
   ```bash
   npm install tailwindcss @tailwindcss/cli
    